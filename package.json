{
  "name": "forge",
  "version": "0.0.1",
  "private": "true",
  "license": "MIT",
  "scripts": {
    "build:apps": "yarn install && cd db && npx prisma generate && cd ../backend && yarn test && yarn lint && yarn build && cd ../ui yarn lint && yarn build"
  },
  "workspaces": [
    "ui",
    "backend",
    "db",
    "dto"
  ]
}
