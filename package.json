{
  "name": "forge",
  "version": "0.0.1",
  "private": "true",
  "license": "MIT",
  "scripts": {
    "build:libs": "yarn install && cd dto && npx tsc && cd ../db && npx prisma generate ",
    "build:apps": "yarn install && cd backend && yarn test && yarn lint && yarn build && cd ../ui yarn lint && yarn build"
  },
  "workspaces": [
    "ui",
    "backend",
    "db",
    "dto"
  ]
}
